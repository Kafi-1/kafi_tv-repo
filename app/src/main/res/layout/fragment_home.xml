<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_black">

    <!-- Search Bar -->
    <EditText
        android:id="@+id/etSearch"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_margin="16dp"
        android:background="@drawable/bg_card"
        android:hint="@string/search_hint"
        android:textColorHint="@color/text_hint"
        android:textColor="@color/text_primary"
        android:textSize="16sp"
        android:gravity="center"
        android:paddingHorizontal="20dp"
        android:inputType="text"
        android:imeOptions="actionSearch"
        android:maxWidth="600dp"
        android:layout_gravity="center_horizontal"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:nextFocusDown="@id/rvCategories" />

    <!-- Expiry Warning (hidden by default) -->
    <LinearLayout
        android:id="@+id/expiryWarning"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/bg_expiry_box"
        android:padding="24dp"
        android:layout_margin="16dp"
        android:gravity="center"
        android:visibility="gone">

        <TextView
            android:id="@+id/tvExpiredTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/subscription_expired"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary"
            android:textAllCaps="true" />

        <TextView
            android:id="@+id/tvExpiredMsg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/default_expire_msg"
            android:textSize="@dimen/text_md"
            android:textColor="@color/text_secondary"
            android:layout_marginTop="8dp" />

        <Button
            android:id="@+id/btnRenewNow"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/btn_renew_now"
            android:background="@drawable/bg_btn_primary"
            android:textColor="@color/text_primary"
            android:textStyle="bold"
            android:textSize="@dimen/text_sm"
            android:textAllCaps="true"
            android:paddingHorizontal="24dp"
            android:paddingVertical="12dp"
            android:layout_marginTop="16dp"
            android:focusable="true" />
    </LinearLayout>

    <!-- Category Bar -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvCategories"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="12dp"
        android:paddingBottom="16dp"
        android:clipToPadding="false"
        android:scrollbars="none"
        android:focusable="true"
        android:nextFocusUp="@id/etSearch"
        android:nextFocusDown="@id/rvChannels"
        android:descendantFocusability="afterDescendants" />

    <!-- Channel Grid -->
    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefresh"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvChannels"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingHorizontal="8dp"
            android:clipToPadding="false"
            android:scrollbars="vertical"
            android:focusable="true"
            android:nextFocusUp="@id/rvCategories"
            android:descendantFocusability="afterDescendants" />
    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>
</LinearLayout>
